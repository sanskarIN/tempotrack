# TempoTrack has no required secrets or network credentials.
# Keep this file as the canonical placeholder if future optional integrations
# introduce environment configuration. Never commit real secrets.
