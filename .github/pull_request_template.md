## What changed

<!-- Describe the user-visible and technical changes. -->

## Verification

- [ ] `python tools/check_release_metadata.py` passes.
- [ ] `python tools/check_gradle_version_alignment.py` passes.
- [ ] `python tools/check_kotlin_package_keywords.py` passes.
- [ ] `python tools/check_repository_reference.py` passes.
- [ ] `python tools/check_markdown_links.py` passes.
- [ ] Relevant unit tests pass.
- [ ] `./gradlew quality` passes, or the limitation is explained below.
- [ ] Platform build affected by this PR succeeds.
- [ ] Manual lifecycle/export/share/accessibility checks were run when the change affects those boundaries, or the limitation is explained below.
- [ ] No secrets, private data, or signing material were added.
- [ ] Documentation/changelog updated when needed.
- [ ] `docs/repository-reference.md` was updated for every tracked-file addition/removal/rename.
- [ ] Accessibility impact considered.

## Screenshots

<!-- Add before/after screenshots for meaningful UI changes only when captured from a real verified build. -->

## Notes / limitations

<!-- Include any intentionally deferred follow-up and any verification that could not actually be run. -->
