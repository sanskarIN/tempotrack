name: Release Build

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
      - uses: android-actions/setup-android@v3
      - run: sdkmanager "platforms;android-37" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.5.0"
      - name: Verify and build Android release
        run: gradle :shared:allTests :androidApp:lintRelease :androidApp:testDebugUnitTest :androidApp:assembleRelease --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: tempotrack-android-release-unsigned
          path: androidApp/build/outputs/apk/release/*.apk

  desktop:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.5.0"
      - shell: bash
        run: gradle :shared:allTests :desktopApp:packageDistributionForCurrentOS --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: tempotrack-desktop-${{ runner.os }}
          path: desktopApp/build/compose/binaries/main/**
